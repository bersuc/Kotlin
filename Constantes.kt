package fundamentos

fun main(args: Array<String>) {
    // val é uma constante, ou seja, não se modifica nunca.. será sempre o mesmo valor.
    val raio = 4.5

    println(Math.floor(raio * raio * Math.PI))
    println("vc é linduuu")
}